{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "PORT=8000 nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.3"
  },
  "dependencies": {
    "html-validate": "^7.1.1",
    "html-validate-es": "^0.2.0"
  }
}

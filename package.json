{
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "dev": "PORT=8000 nuxi dev",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "start": "PORT=80 HOST=0.0.0.0 node .output/server/index.mjs",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "html-validate": "^7.1.1",
    "html-validate-es": "^0.2.0"
  }
}
